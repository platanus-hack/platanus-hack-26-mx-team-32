{
  // Your project's name, as it will appear on the voting page.
  "project-name": "Sendero",

  // Short one-line summary of your project (in Spanish).
  "project-oneliner-spanish": "Capa conectiva forense que cruza fichas de búsqueda con cuerpos no identificados vía señas particulares (BNDF).",

  // Brief description of your project (in Spanish).
  "project-description-spanish": "Sendero es un motor de record-linkage forense para la crisis de personas desaparecidas en México. Resuelve señas particulares (tatuajes, cicatrices, lunares) a vocabulario controlado del Banco Nacional de Datos Forenses para que 'ancla brazo der.' (SEMEOF) coincida con 'tatuaje de áncora en antebrazo derecho' (ficha). Arquitectura dual-plane: bloqueo → scoring por campo → verificador separado que prioriza para revisión humana y nunca concluye match. La lateralidad descarta near-misses. Demo punta a punta sobre datos sintéticos calibrados por el RNPDNO real + contexto agregado real (fosas, tendencias). RBAC estricto: una sesión readonly no puede leer coordenadas de fosas.",

  // Public URL where your project is deployed and running.
  // Must be a valid URL, for example: https://my-project.vercel.app
  // If your project is an app, put the download link here instead
  // (for example, a URL to download an APK).
  "deploy-url": "https://team32.vercel.app/home"
}
